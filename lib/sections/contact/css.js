// Redaxo CSS: Kontakt

export default `
/* Kontakt */
.section--contact {
  background: var(--color-surface-alt);
}
.contact-grid {
  max-width: 600px;
  margin: 0 auto;
}
.contact-info {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}
.contact-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  font-size: 1.1rem;
}
.contact-item__icon {
  font-size: 1.5rem;
}
`
