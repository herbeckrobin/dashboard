// Redaxo CSS: Kundenstimmen

export default `
/* Kundenstimmen */
.card--testimonial {
  text-align: center;
}
.testimonial__quote {
  font-size: 1.1rem;
  font-style: italic;
  color: var(--color-text-muted);
  margin-bottom: 1.5rem;
  line-height: 1.7;
}
.testimonial__role {
  display: block;
  color: var(--color-text-muted);
  font-size: 0.9rem;
  margin-top: 0.25rem;
}
`
