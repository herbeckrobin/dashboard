// Redaxo CSS: Call to Action

export default `
/* Call to Action */
.section--cta {
  text-align: center;
  background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
  color: var(--color-text-inverted);
}
.cta__title {
  font-size: 2.25rem;
  font-weight: 700;
  margin-bottom: 1rem;
}
.cta__subtitle {
  font-size: 1.15rem;
  opacity: 0.9;
  margin-bottom: 2rem;
  max-width: 600px;
  margin-left: auto;
  margin-right: auto;
}
.section--cta .btn--primary {
  background: var(--color-bg);
  color: var(--color-primary);
}
`
